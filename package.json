{
  "name": "@bounoable/on-click-outside",
  "version": "0.1.0",
  "description": "Detect clicks outside of an element.",
  "main": "dist/index.esm.js",
  "repository": "https://github.com/bounoable/on-click-outside",
  "author": "Saman Hosseini <bounoable@gmail.com>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "jsdom": "^19.0.0",
    "vite": "^2.9.9",
    "vitest": "^0.14.1"
  },
  "scripts": {
    "watch": "vitest",
    "test": "vitest run",
    "build": "vite build && pnpm generate-types",
    "generate-types": "tsc --project ./tsconfig.json"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
